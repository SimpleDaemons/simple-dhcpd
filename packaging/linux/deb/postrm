#!/bin/bash
# Post-removal script for simple-dhcpd

set -e

PROJECT_NAME="simple-dhcpd"
CONFIG_DIR="/etc/$simple-dhcpd"
LOG_DIR="/var/log/$simple-dhcpd"

if [ "$1" = "purge" ]; then
    # Remove configuration files (optional - user may want to keep them)
    # Uncomment to remove config on purge:
    # rm -rf "$CONFIG_DIR"
    
    # Remove log files
    rm -rf "$LOG_DIR"
    
    # Remove service user (optional)
    # userdel dhcpddev 2>/dev/null || true
fi

exit 0

